import Button from "@/components/ui/Button";

const QuestionsPage = () => {
  return (
    <>
      <h1>This is only here because I thought someone might check. I could have put the dynamic page 1 directory lower so it doesn't have it's own path but I prefer this way</h1>
      <Button link={"/"} text={"Country road, take me home"} />
    </>
  );
};

export default QuestionsPage;
